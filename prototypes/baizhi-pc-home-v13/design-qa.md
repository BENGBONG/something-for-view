# Agent 任务页：Figma 规范核对

日期：2026-09-11。结果：本轮原型核对通过，不代表业务接口或生产验收。

## 依据

用户指定 `wwphF7kndk3A7a2j4D7NbO / 25042:4856`，该节点是“百智PC重构”整页。首次设计上下文返回“未选中图层”后，读取页面结构，定位并读取以下具体画面的设计上下文：

- `25132:63710`：任务模块，提供页面、按钮、字体、表格与侧栏规范。
- `25095:34799`：首页-小智，提供 410px 右侧面板、输入区及相关图标规范。

任务结果内容是按这些规范适配的新增设计，不宣称存在完全同构的 Figma 任务结果稿。

## 视觉证据

- Figma 原图：`output/figma/baizhi-panel-25095-34799.png`（1680×900）。
- 同尺寸原型：`output/playwright/apps-task-figma-wide.png`（1680×900）。
- 常规窗口：`output/playwright/apps-task-figma.png`（1092×988）。
- 原图与同尺寸截图在同一次工具输出中并排核对；浏览器尺寸测试后已恢复。

## 核对结果

- 背景：主内容纯白；侧栏 #F9F9F9；细分隔线 #F0F0F0。
- 字体：PingFang SC；正文 14/24，标题 16/28，辅助文案 12/18。
- 尺寸：标准画布侧栏 260px、顶部 66px、工作台 410px；窄桌面 220px / 380px，文档没有横向溢出。
- 组件：黑色主要按钮、短下划线页签、32px 操作按钮、12px 卡片圆角、16px 输入区圆角。去掉前版大面积蓝紫色、浮动圆角工作台和重复的卡片容器。
- 资源：四个替换图标使用 Figma 原始导出文件；无缺失图片。
- 内容：固定更新摘要、多表、前后值与最新数据保留；不增加文件导出、WPS 编辑或新数据范围入口。

## 交互与修正

- 中间“查看最新数据”正确切换右侧视图；商机跟进显示最新金额 350,000。本次变更仍显示 280,000 → 350,000。
- 新任务视图和普通任务视图使用同一套设计规范。发现并修正欢迎区 Agent 名称与侧栏不一致的问题。
- 普通任务引用来源原本出现浏览器默认按钮，已改为列表行；进度条统一为中性色；窄屏文件列表单列显示。
- 本轮浏览器 error 日志为空；JS 语法检查和 git diff 空白检查通过。

## 范围边界

### 本轮补充：紧凑卡片

按用户提供的原有文件卡片参考，应用卡片改为横向两行信息条目。1092×988 窗口实测高度 78px，无页面横向溢出；查看本次变更、查看最新数据两个操作均可切换工作台。移除大数字统计区与独立时间行，右侧工作台未改动。上方截图路径记录的是此前的 Figma 整页版本，不作为紧凑卡片的新截图。

- 保留既有侧栏菜单结构，没有照搬首页导航或新增功能。
- 数据、任务执行、文件和权限仍为现有原型模拟；未接入后端。
- 本记录及设计说明随原型提交 GitHub；主 PRD 在项目工作区单独维护，不在本仓库发布。
