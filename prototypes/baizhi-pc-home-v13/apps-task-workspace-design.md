# 任务结果页：应用更新（v4.0 · 2026-09-17）

## 本次范围
任务结果仍采用中间紧凑卡片、右侧工作台。业务内容变更历史取消，仅表结构历史保留。
一个实际更新的 App 一张卡片，沿用 Figma 的字体、间距、白色主内容和品牌蓝链接，不改全局布局。

## 中间卡片
- 名称、已更新状态、涉及表摘要；不再展示业务记录新增/修改/删除计数。
- 普通数据更新只提供“查看最新数据”，工作台默认打开最新表格。
- 只有确实生效的结构变化才增加“查看结构变更”；比较的是字段名称、类型等定义。
- 仅绑定/只读的 App 不列出；任务失败按真实状态说明已生效部分，不生成虚构历史。

## 右侧工作台
保留“工具调用 / 文件列表 / 修订文件 / 应用数据”。
最新数据可切 App 和当前表；结构视图只在本次有结构变化时出现，不展示业务旧值与新值。
“在知识库中打开”分别进入最新数据或对应结构记录。删除表的历史不依赖当前表仍在导航中。
应用无权限时不开放内容；任务权限仍独立判断。

## 与发布应用的关系
“专家·技能·连接器 → 应用”是独立前端页面目录，不放进任务文件列表。
只展示当前已发布且可访问的 App，一张卡片对应平台正式 URL，新标签打开；不使用草稿预览地址。
原型使用 published-app-demo.html 演示跳转，明确标注并非真实平台发布结果。

## 保留的视觉规范
参照用户指定百智 Figma 页面 25042:4856 中的任务模块 25132:63710 与首页 25095:34799。
白色主内容、浅灰侧栏、细分隔线；主要文字 #262626，辅助文字 #8C8C8C，链接 #165DFF。
紧凑卡片沿用约 78px 高度，取消大面积统计展示。原始导出图标位于 assets/figma-task。

## 原型与正式能力
样例演示普通业务更新任务 run-0908-review，以及结构调整任务 run-schema-sales。
平台配置的结构记录不伪造任务入口；任务触发记录可返回原任务。
真实身份、结构历史、发布目录、URL 和权限接口未接入，需求以工作区 PRD v4.0 为准。
本次本地待评审，未推送远端。
