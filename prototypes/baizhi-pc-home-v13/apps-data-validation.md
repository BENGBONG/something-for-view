# 应用数据原型验证记录

## 2026-09-18：双击单元格编辑调整

- 参考本地 Agent Platform `origin/feat/apps-supabase` 的 `static/apps-app.jsx`：`AppDataCell` 双击/Enter 进入编辑，`AppDataEditor` Enter/失焦保存、Esc 取消。
- 原型移除行内“编辑”及保存/取消按钮，仅被双击的业务单元格显示输入控件；所有人、更新时间等系统信息仍不可编辑。
- 真实浏览器已验证：双击“所在区域”仅打开一个编辑框；Enter 保存为“上海市”；再次输入后 Esc 取消仍为“上海市”；重新输入“上海”并点击标题失焦保存成功，测试值恢复。
- 保留原有权限检查、版本冲突保护及新增抽屉。为避免丢失用户输入，保存失败时保留草稿，不照搬平台失败后关闭输入框的行为。
- 编辑器及 UI 的 `node --check`、`git diff --check` 通过。仅本地原型，未接真实接口、未推送。

## 2026-09-18：手动编辑页面评审

本地原型，不调用真实 Apps 服务，不推送线上。

- 浏览器验证：自己的记录可进入行内编辑；修改后切换能力中心弹出离开确认，“保存并离开”保存成功后进入目标模块，返回可见新内容、所属人不变。
- 新增抽屉：空客户名称无法保存，补充内容后新增到表中；新增测试记录删除确认后移除，列表数量恢复。
- 只读表“周期汇总”无新增、编辑、删除入口；管理员企业视图可见张伟的数据但只能只读。
- 修正新增抽屉高度、内边距和固定底部按钮，目视核对最终布局。
- 模型检查：本人增改删、他人操作拒绝、只读表拒绝、手动及 Agent 更新后的过期版本冲突、本地保存失败回滚、手动操作不增加任务历史均通过。
- 新增/修改脚本语法及 git diff 格式检查通过。前端权限是演示规则，真实租户隔离、鉴权和写入校验仍需服务端接入。

## 2026-09-17 v4.0：结构历史与发布应用目录

范围：本地静态原型，真实浏览器验证；未接入 Agent Platform 服务，未推送线上。

- 应用详情的“结构变更历史”仅展示表及字段定义变化，旧业务记录的新增、修改、删除内容不再提供历史入口。
- 结构记录包含版本、来源、操作人、定义差异；包含已删除表的样例，不依赖当前数据表列表。
- 从“客户等级名称 / 预计金额类型”的结构记录进入对应 Agent 任务，任务及工作台展示结构差异；返回恢复原结构详情。
- 普通商机复盘任务仍能查看当前最新表格数据，不再显示内容变更对比或“查看本次变更”按钮。
- “AI专家·技能·连接器”新增“应用”Tab；直接 URL 可定位该 Tab。三张已发布应用示例卡片可见，未发布样例不显示。
- 应用搜索无匹配结果显示空态，清空后恢复卡片。点击“销售洞察”在新标签页打开对应示例 URL，原目录保持不变。
- 目视核对应用卡片和页签布局；新增及修改的脚本语法、HTML 内联脚本与差异格式检查通过。

真实发布 URL、发布目录授权、服务端结构日志、撤权与下架同步尚未接入。示例链接明确标注为本地演示页面。当前需求以工作区 PRD v4.0 为准。

## 2026-09-18 基础表格补齐（本地）

- 浏览器验证：地区筛选为上海后显示 3 条；叠加两个排序字段后保持筛选结果，支持清空条件。第二页显示第 8 条记录，输入页码可返回第一页。
- 勾选两条记录后显示“已选 2 条（当前页）”；删除确认列出准确名称，取消后数据不变。没有在浏览器中删除现有评审样例。
- 收起/展开表导航、记录信息开关正常；记录 ID 与创建时间只读，紧凑网格及横向滚动布局已目视检查。
- 跟进计划 JSON 单元格可双击编辑，无效 JSON 显示错误并阻止保存，Esc 恢复原值；日期时间、UUID、JSON 样例正常显示。
- 隔离内存模型检查通过：组合筛选、多字段排序、日期/UUID/JSON/数值/必填校验，false 与 0 保留；批量删除拒绝他人记录和过期版本，存储失败回滚，成功批量删除计数正确；JSON 字符串值与创建时间保存正确。
- 未加入 AI 字段、公开/私有切换、建表及结构编辑。新增功能是浏览器本地模拟，未接真实服务，未同步 PRD 或推送远端。

## 2026-09-20 能力中心按版本区分（本地）

- 个人版使用 `page=capabilities&tab=experts` 旧链接时，页面回退到技能；侧栏和页头为“技能·连接器·应用”，没有专家页签、分类、搜索或卡片。
- 个人版“应用”页签正常显示已发布应用卡片。
- 通过账号菜单切换到企业版，再打开能力中心，默认展示专家与四个页签；侧栏及页头为“Ai 专家·技能·连接器·应用”。截图核对菜单完整显示。
- 版本选择页的个人版说明同步移除专家；HTML 内联脚本语法及差异格式检查通过。本轮未推送远端。

## 2026-09-20 只读样式、表格下载、任务入口

- 只读业务单元格、所属人、更新时间和可选系统字段采用 `#e8ecf2` 底色并带只读说明。管理员查看他人页时 0 个可编辑业务单元格，切回本人页恢复可编辑；周期汇总整表只读。
- 实际通过页面下载并解析 XLSX：客户画像共 8 条本人记录，包含第 2 页，未包含其他所属人；搜索上海后下载 3 条，均属于张伟且区域上海。下载沿用当前查询函数，覆盖全部匹配记录而非分页切片；写入保护沿用现有编辑守卫。
- `node tests/apps-table-export.test.cjs` 验证 ZIP/CRC、XML、中文、0/false、JSON、长数字文本及公式字符串安全；空表可导出表头。
- 从首页历史任务点击“复盘华东销售商机，更新客户跟进状态”，已进入对应 Agent 结果页，应用更新卡片、右侧应用数据工作台和知识库链接正常展示。
- 页面无运行错误，修改的脚本语法与 `git diff --check` 通过；钉钉 PRD 未在本轮修改。

## 以下为历史版本验证，不代表当前功能范围

下文涉及业务内容前后值、删除前值及业务操作历史的检查已随 v4.0 取消，仅保留为历史记录。

## 2026-09-11 任务详情跳转增量验证

本机 HTTP 原型通过真实浏览器验证：历史“查看本次任务”离开 index iframe，进入对应运行的 agent.html；任务名称、Agent 和结果匹配。返回后保留“修改”筛选及选中变更抽屉。较早的多应用任务分别显示销售洞察和客户商机；周宁失败任务保留失败状态和已提交更新；张伟直接打开周宁任务被阻断。上述流程无 JavaScript pageerror，脚本语法及 diff 检查通过。

重复任务摘要弹窗已移除。管理者查看他人完整任务、真实任务服务权限、滚动位置及历史视图过期恢复仍未完成正式验收。样例只为触发人提供任务查看权限，不代表最终任务授权规则。完整执行日志未接入。

## 2026-09-10 历史验证记录

下列为原有记录，不等于当前全部问题已解决。审查曾发现管理者任务追溯范围丢失、任务入口重复摘要、历史字段随当前定义等问题；最新处理情况以上方增量记录及 README 为准。页头演示设置和列表范围 Tab 已移除，不能再按旧 UI 切身份执行场景。

日期：2026-09-10。范围：本机 HTTP 静态原型，Playwright/Chromium，1440×900 原始页面及 index iframe 入口。不是后端能力验收。

## 已通过

- 张伟只有销售洞察/客户商机，不混入周宁记录；三张表，客户画像 8 条，分页 7+1。
- 最新商机金额为 350,000；历史可查 280,000→350,000，并保留删除前值。
- 陈琳只管理自己创建的两个应用；管理范围客户画像 35 条，筛选周宁 9 条。
- 李敏在企业版可管理三个应用；个人版只保留其创建的项目交付，不套用企业管理员权限。
- 普通新成员许安初始空列表。只读成功与部分写入后失败均不新增入口；无变化不提示数据更新。
- 随后成功写项目交付，入口自动出现；保留早先失败时提交的数据，两次提交分别可追溯。
- 空风险表保留字段及空态。
- 单次销售洞察 Agent 更新销售洞察/客户商机，结果显示两个 App；商机复盘 Agent 再更新销售洞察，不产生重复入口。
- 从任务结果进入固定 run 的变更详情，定位 run-0908-review；新任务结果不出现保存至 Agent 产物。
- 应用名称/表数据搜索、历史操作类型与日期筛选正确；变更抽屉与筛选统计一致。
- 普通 DOCX 编辑时进入应用数据，触发保存/放弃/继续确认；继续编辑不离开，放弃后正确进入应用数据。
- 首页、普通文件导航仍可使用；应用数据不出现旧 Agent 文件夹。
- app.html / agent.html / index.html 内联脚本及新增 JS 语法检查通过；git diff --check 通过。浏览器交互中无 JavaScript pageerror。旧 index 入口的 favicon.ico 404 不影响上述交互。

## 评审截图

项目根目录 `output/playwright/`：apps-data-list.png、apps-data-tables.png、apps-data-history.png。

## 未模拟/不可由此证明

真实登录与企业租户隔离、数据库行级授权、可靠审计落库、并发写入、生产实时通知、跨页一致性、App 暂停/删除/撤权及服务端故障。原型数据及身份均为本地演示。正式要求以工作区 PRD v3.6 为准，不代表生产已经支持。

原始验证时未推送；本次将静态原型同步至 GitHub Pages。旧 Agent 产物验证仅为历史记录。

## 2026-09-14 多表导航与紧凑网格

- 应用内左侧导航显示客户画像、商机跟进、周期汇总，选中态与右侧标题、表头和记录一致。
- 客户画像分页 7+1 正常；从第二页切商机跟进后回到第一页，金额显示 350,000。
- 搜索不存在内容显示空结果；切周期汇总后清空搜索，正常显示 620,000 和行动建议。
- 截图核对：右侧表格收紧行高和列宽、保留姓名及部门，细网格线可见；应用级左侧菜单未改。
- 更新历史仍使用原列表和筛选布局，未套用新的表导航。
- 同批包含上一轮任务结果无实际更新的文案修正；脚本语法与差异格式检查通过。

上述为本地浏览器验证；没有重复执行原有权限全量验收，不代表真实服务已接入。
